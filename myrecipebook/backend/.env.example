# OpenAI API キー（未設定でもモックで動作します）
OPENAI_API_KEY=

# DB（デフォルトはSQLite。PostgreSQLに切り替える場合は以下をコメント解除）
# DATABASE_URL=postgresql://user:password@localhost:5432/myrecipebook
DATABASE_URL=sqlite:///./recipes.db
